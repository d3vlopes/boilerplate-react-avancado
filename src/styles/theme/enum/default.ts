export enum Grid {
  container = '130rem',
  gutter = '3.2rem',
}

export enum Radius {
  radius = '0.4rem',
}

export enum Family {
  main = "Poppins, -apple-system, BlinkMacSystemFont, 'Segoe UI', Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif",
}

export enum Weight {
  light = 300,
  normal = 400,
  bold = 600,
}

export enum Sizes {
  xsmall = '1.2rem',
  small = '1.4rem',
  // Tamanho base
  medium = '1.6rem',
  large = '1.8rem',
  xlarge = '2.0rem',
  xxlarge = '2.8rem',
  huge = '5.2rem',
}

export enum Colors {
  primary = '#F231A5',
  secondary = '#3CD3C1',
  mainBg = '#06092B',
  lightBg = '#F2F2F2',
  white = '#FAFAFA',
  black = '#030517',
  lightGray = '#EAEAEA',
  gray = '#8F8F8F',
  darkGray = '#2E2F42',
  red = '#FF6347',
}

export enum Spacings {
  xxsmall = '0.8rem',
  xsmall = '1.6rem',
  small = '2.4rem',
  medium = '3.2rem',
  large = '4.0rem',
  xlarge = '4.8rem',
  xxlarge = '5.6rem',
}

export enum Layers {
  base = '10',
  menu = '20',
  overlay = '30',
  modal = '40',
  alwaysOnTop = '50',
}

export enum Transition {
  default = '0.3s ease-in-out',
  fast = '0.1s ease-in-out',
}
